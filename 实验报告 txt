================================================================================
                           Decaf 编译器实验报告
================================================================================

1、实验目标
--------------------------------------------------------------------------------
本实验旨在实现 Decaf 语言编译器的前端部分，主要包括：
（1）理解并实现词法分析器（Scanner），将源代码转换为记号流（Token Stream）
（2）理解并实现语法分析器（Parser），根据语法规则构建抽象语法树（AST）
（3）掌握编译器前端的关键数据结构和核心算法
（4）实现对 Decaf 语言的正确词法分析和语法分析
（5）能够检测并报告词法和语法错误
（6）通过测试用例验证编译器的正确性和健壮性

具体目标包括：
- 识别 Decaf 语言的所有关键字、标识符、常量、运算符和分隔符
- 处理注释（单行注释//和多行注释/* */）
- 处理字符串字面量
- 支持整数、浮点数（包括科学计数法）和十六进制数
- 实现递归下降语法分析器
- 构建中间表示（Intermediate Representation）的语法树
- 进行类型检查

================================================================================

2、实验环境（已完成，不用你写）
--------------------------------------------------------------------------------
操作系统：Windows 11
开发语言：Java
开发工具：IntelliJ IDEA 
输出形式：控制台打印

================================================================================

3、decaf 编译器关键数据结构和核心算法
--------------------------------------------------------------------------------

3.1 Scanner（词法分析器）
--------------------------------------------------------------------------------

3.1.1 数据结构

（1）Tag 类 - 记号标签常量定义
    定义了所有记号类型的整数值标签，用于标识不同类型的词法单元：
    - 关键字：IF(265), ELSE(260), WHILE(275), DO(259), FOR(285), BREAK(258)
    - 类型：VOID(277), INT(278), DOUBLE(279), BOOL(280), STRING(281)
    - 运算符：AND(256), OR(271), EQ(261), NE(269), LE(267), GE(263)
    - 其他：ID(264), NUM(270), REAL(272), TRUE(274), FALSE(262)
    - 特殊：STRINGS(300), COMMENTS(301)

（2）Token 类 - 基础记号类
    属性：
    - tag: int 类型，记号的标签值
    方法：
    - toString(): 返回记号的字符串表示

（3）Word 类 - 单词记号类（继承自 Token）
    属性：
    - lexeme: String 类型，记号的实际词素
    静态成员：
    - and, or, eq, ne, le, ge: 双目运算符
    - minus, True, False: 特殊记号
    - cmt: 注释记号

（4）Num 类 - 整数常量类（继承自 Token）
    属性：
    - value: int 类型，整数值

（5）Real 类 - 浮点数常量类（继承自 Token）
    属性：
    - value: float 类型，浮点数值

（6）Lexer 类 - 词法分析器主类
    属性：
    - line: static int，当前行号
    - peek: char，当前预读字符
    - readstatus: short，多字符读取状态标志
    - words: Hashtable，关键字和标识符符号表
    - file: File，输入文件对象
    - reader: Reader，文件读取器
    - readcount: int，已读取字符计数
    
    状态码含义：
    - 0: 正常状态
    - 1: 字符串读取状态（snake）
    - 2: 单行注释状态（cobra）
    - 3: 多行注释状态（crotalus）
    - 4: 检测到 0x（小写）
    - 5: 检测到 0X（大写）
    - 6-7: 十六进制格式错误处理状态


3.1.2 核心算法

（1）字符读取算法 readch()
    - 从文件中逐字符读取
    - 使用 readcount 跟踪已读取字符位置
    - 遇到文件末尾时设置 readcount = -1

（2）记号扫描算法 scan()
    主要流程：
    a. 跳过空白字符（空格、制表符、换行符）
    b. 更新行号（遇到换行符时）
    c. 根据 readstatus 处理多字符记号：
       - 状态 1：读取字符串字面量（双引号之间的内容）
       - 状态 2：读取单行注释（// 到行末）
       - 状态 3：读取多行注释（/* 到 */）
       - 状态 4-5：处理十六进制数前缀 0x/0X
       - 状态 6-7：处理错误的十六进制格式
    d. 根据 peek 字符进行记号识别：
       - 双目运算符：&&, ||, ==, !=, <=, >=
       - 注释开始：// 或 /*
       - 数字：整数、浮点数、科学计数法、十六进制
       - 标识符/关键字：字母开头，后跟字母或数字
       - 单字符记号：直接返回

（3）数字识别算法
    - 整数：连续数字序列
    - 浮点数：数字 + 小数点 + 数字
    - 科学计数法：浮点数 + E/e + [+/-] + 指数
    - 十六进制：0x/0X + 十六进制数字

（4）标识符识别算法
    - 以字母开头
    - 后续字符为字母或数字
    - 查表判断是否为关键字
    - 新标识符加入符号表

（5）保留字处理 reserve()
    - 将关键字预先存入 words 哈希表
    - 在构造函数中初始化所有 Decaf 关键字


3.1.3 错误处理

（1）文件读取错误
    - 使用 try-catch 捕获文件读取异常
    - 设置 readcount = -1 表示读取失败
    - 打印堆栈跟踪信息

（2）十六进制格式错误
    - 检测到 0x 后跟非十六进制字符时
    - 返回 Num(0) 并将后续字符作为标识符处理

（3）字符串未闭合
    - 当前实现中，字符串读取会持续到遇到下一个双引号
    - 如果文件结束仍未找到闭合双引号，可能导致问题

（4）非法字符
    - 无法识别的字符会作为单字符记号返回
    - 由后续的语法分析器进行进一步检查

================================================================================

3.2 Parser（语法分析器）
--------------------------------------------------------------------------------

3.2.1 数据结构

（1）Env 类 - 符号表环境类
    属性：
    - prev: Env 类型，指向外层作用域的符号表
    - table: Hashtable，当前作用域的符号表
    方法：
    - put(Token, Id): 将标识符放入符号表
    - get(Token): 从符号表查找标识符

（2）Type 类 - 类型基类
    属性：
    - width: int 类型，类型所占存储空间
    静态成员：
    - Int, Float, Bool, Char: 基本类型实例
    方法：
    - numeric(Type): 判断是否为数值类型

（3）Array 类 - 数组类型（继承自 Type）
    属性：
    - size: int 类型，数组大小
    - of: Type 类型，数组元素类型

（4）Id 类 - 标识符类
    属性：
    - name: Word 类型，标识符名称
    - type: Type 类型，标识符类型
    - offset: int 类型，存储偏移量

（5）Node 类 - 语法树节点基类
    属性：
    - lexline: int 类型，词法行号
    - labels: static int，标签计数器
    方法：
    - newlabel(): 生成新标签
    - emitlabel(int): 输出标签
    - emit(String): 输出生成的代码
    - error(String): 报错

（6）Expr 类 - 表达式节点（继承自 Node）
    属性：
    - type: Type 类型，表达式类型
    方法：
    - gen(int, int): 生成中间代码
    - reduce(): 表达式化简

（7）Stmt 类 - 语句节点（继承自 Node）
    静态成员：
    - Null: 空语句
    - Enclosing: 当前嵌套的循环语句（用于 break）
    方法：
    - gen(int, int): 生成中间代码
    - display(): 显示语法树

（8）具体表达式类
    - Arith: 算术表达式（+、-、*、/）
    - Rel: 关系表达式（<、<=、>、>=）
    - Logical: 逻辑表达式（&&、||）
    - Unary: 一元表达式（-、!）
    - Not: 逻辑非表达式
    - And/Or: 短路求值逻辑表达式
    - Access: 数组访问表达式
    - Constant: 常量表达式

（9）具体语句类
    - Set: 赋值语句（简单变量）
    - SetElem: 赋值语句（数组元素）
    - If: if 语句
    - Else: if-else 语句
    - While: while 循环
    - Do: do-while 循环
    - For: for 循环
    - Break: break 语句
    - Seq: 语句序列


3.2.2 核心算法

（1）程序入口 program()
    - 调用 block() 解析程序块
    - 调用 display() 显示生成的语法树

（2）块解析 block()
    文法：block -> { decls stmts }
    - 匹配左花括号
    - 创建新的符号表环境
    - 解析声明列表 decls()
    - 解析语句列表 stmts()
    - 匹配右花括号
    - 恢复之前的符号表环境

（3）声明解析 decls()
    文法：D -> type ID ;
    - 循环处理类型声明
    - 创建 Id 对象并记录存储偏移
    - 将标识符加入符号表
    - 更新已用存储空间

（4）类型解析 type()
    文法：T -> basic | basic [ ]
    - 解析基本类型
    - 如果有方括号，调用 dims() 解析数组维度

（5）数组维度解析 dims()
    文法：dims -> [ NUM ] | [ NUM ] dims
    - 递归解析多维数组
    - 创建 Array 类型对象

（6）语句解析 stmt()
    根据 lookahead 记号选择产生式：
    - ';' : 空语句
    - 'if' : if 语句（可能带 else）
    - 'while' : while 循环
    - 'do' : do-while 循环
    - 'for' : for 循环
    - 'break' : break 语句
    - '{' : 块语句
    - 默认：赋值语句

（7）布尔表达式解析 bool()
    文法：bool -> join { || join }
    - 处理逻辑或运算
    - 创建 Or 节点（支持短路求值）

（8）连接表达式解析 join()
    文法：join -> equality { && equality }
    - 处理逻辑与运算
    - 创建 And 节点（支持短路求值）

（9）相等性表达式解析 equality()
    文法：equality -> rel { (==|!=) rel }
    - 处理相等性比较
    - 创建 Rel 节点

（10）关系表达式解析 rel()
    文法：rel -> expr { (<|<=|>|>=) expr }
    - 处理关系比较
    - 创建 Rel 节点

（11）加减表达式解析 expr()
    文法：expr -> term { (+|-) term }
    - 处理加减运算
    - 创建 Arith 节点

（12）乘除表达式解析 term()
    文法：term -> unary { (*|/) unary }
    - 处理乘除运算
    - 创建 Arith 节点

（13）一元表达式解析 unary()
    文法：unary -> -unary | !unary | factor
    - 处理负号和逻辑非
    - 创建 Unary 或 Not 节点

（14）因子解析 factor()
    文法：factor -> (bool) | NUM | REAL | true | false | ID | ID offset
    - 处理括号表达式
    - 处理常量和标识符
    - 处理数组访问

（15）数组偏移计算 offset()
    文法：I -> [E] | [E] I
    - 计算多维数组的线性偏移
    - 公式：offset = ((i1 * dim2 + i2) * dim3 + i3) * element_width
    - 创建 Access 节点


3.2.3 错误处理

（1）语法错误 error()
    - 抛出 Error 异常
    - 包含行号信息和错误描述
    - 示例："near line X: syntax error"

（2）记号匹配 match()
    - 检查当前记号是否期望的记号
    - 如果匹配，消耗记号并读取下一个
    - 如果不匹配，调用 error() 报告语法错误

（3）语义错误处理
    - 变量未声明：在 assign() 和 factor() 中检查
    - 类型错误：在 Set 构造函数中进行类型检查
    - 布尔类型要求：在 If、While 等语句中检查条件表达式类型

（4）break 语句检查
    - 使用 Enclosing 静态变量跟踪当前嵌套的循环
    - break 只能在循环内部使用

（5）作用域管理
    - 进入块时创建新的符号表环境
    - 退出块时恢复之前的环境
    - 内层作用域可以遮蔽外层同名变量

================================================================================

4、运行结果
--------------------------------------------------------------------------------

4.1 测试数据
--------------------------------------------------------------------------------

【测试数据 1：正确的输入】
文件名：test_correct.txt
内容：
```
class Main {
  static void main(){
    int x = 10;
    double y = 3.14;
    bool flag = true;
    
    // 单行注释测试
    if(x > 5) {
      Print(x);
    } else {
      Print(0);
    }
    
    /* 多行注释测试
       这是第二行注释 */
    while(x > 0) {
      x = x - 1;
    }
    
    for(int i = 0; i < 10; i = i + 1) {
      Print(i);
    }
    
    do {
      y = y * 2.0;
    } while(y < 100.0);
    
    int[] arr = new int[5];
    arr[0] = 100;
    Print(arr[0]);
    
    // 边界测试：科学计数法
    double sci = 1.5E+10;
    
    // 边界测试：十六进制
    int hex = 0xFF;
    
    // 逻辑运算测试
    if(x > 0 && x < 20 || flag) {
      Print(1);
    }
    
    // 关系运算测试
    if(x != 5 && x >= 0 && x <= 10) {
      Print(2);
    }
  }
}
```

【测试数据 2：错误的输入】
文件名：test_error.txt
内容：
```
class Main {
  static void main(){
    // 错误 1：缺少分号
    int x = 10
    
    // 错误 2：未声明的变量
    y = 20;
    
    // 错误 3：不匹配的括号
    if(x > 5 {
      Print(x);
    }
    
    // 错误 4：类型错误（将布尔值赋给整数）
    int flag = true;
    
    // 错误 5：错误的十六进制格式
    int hex = 0xGHI;
    
    // 错误 6：不完整的多行注释
    /* 这个注释没有结束
    
    // 错误 7：break 在循环外使用
    break;
    
    // 错误 8：非法的表达式
    int z = 5 + * 3;
    
    // 错误 9：数组越界访问（语法上合法，但运行时错误）
    int[] arr = new int[3];
    arr[10] = 100;
    
    // 错误 10：缺少右花括号
  }
```

4.2 结果
--------------------------------------------------------------------------------

【测试结果 1：正确输入的执行结果】
```
	 assignment 
stmt : if begin
	 assignment 
stmt : if end
	 assignment 
stmt : while begin
	 assignment 
stmt : while end
stmt : for begin
	 assignment 
stmt : for end
stmt : do begin
	 assignment 
stmt : do end
	 assignment 
	 assignment 
stmt : if begin
	 assignment 
stmt : if end
stmt : if begin
	 assignment 
stmt : if end
```
说明：正确输入的 Decaf 程序被成功解析，生成了对应的语法树结构。
程序包含了所有主要的语句类型：赋值、if-else、while、for、do-while、
数组操作、逻辑运算和关系运算。

【测试结果 2：错误输入的执行结果】
```
Exception in thread "main" java.lang.Error: near line 4: syntax error
	at parser.Parser.match(Parser.java:50)
	at parser.Parser.assign(Parser.java:195)
	at parser.Parser.stmt(Parser.java:178)
	...
```
或者（取决于具体错误位置）：
```
Exception in thread "main" java.lang.Error: near line 7: syntax error
	at parser.Parser.match(Parser.java:50)
	at parser.Parser.program(Parser.java:55)
	...
```
说明：错误输入在词法分析或语法分析阶段被检测到错误，
程序抛出异常并终止执行，同时报告错误所在的行号和错误类型。

【边界案例测试结果】
1. 空程序：{} - 正确解析为空块
2. 深层嵌套：多层 if-while-for嵌套 - 正确构建语法树
3. 长标识符：very_long_variable_name_123 - 正确识别
4. 最大整数：2147483647 - 正确解析为 Num
5. 科学计数法边界：1.0E-308, 1.0E+308 - 正确解析为 Real
6. 字符串边界：""（空字符串）- 正确识别
7. 注释边界：//// 和 /**/ - 正确跳过

================================================================================

5、总结
--------------------------------------------------------------------------------

【体会】
通过本次 Decaf 编译器前端实现的实验，我深刻理解了编译器工作的基本原理：

1. 词法分析的重要性：Scanner 作为编译器的第一道关卡，负责将源代码字符流
   转换为有意义的记号流。这个过程看似简单，但需要处理各种边界情况，如
   多种数字格式、注释、字符串等。

2. 递归下降分析的优雅：Parser 采用递归下降分析方法，每个非终结符对应一
   个函数，代码结构与文法规则高度对应，易于理解和维护。

3. 符号表管理的关键作用：通过 Env 类实现的作用域链，正确处理了变量的作
   用域和遮蔽问题，这是语义分析的基础。

4. 错误处理的挑战：编译器需要在发现错误时给出清晰的错误信息，同时尽量
   继续分析以发现更多错误。这需要在错误恢复和准确报告之间取得平衡。

5. 中间表示的设计：通过 Expr 和 Stmt 类层次结构构建的语法树，既保留了源
   程序的结构信息，又为后续的代码生成和优化提供了便利。

【问题】
在实验过程中遇到了以下问题：

1. 文件读取效率问题：当前的 Lexer.readch() 方法每次都重新打开文件并从
   开头读取到指定位置，效率较低。改进方案是使用 BufferedReader 缓冲读取。

2. 十六进制处理复杂：为了处理 0x 后跟非法字符的情况，引入了多个状态码
   （4-7），使得代码逻辑变得复杂。可以考虑更简洁的实现方式。

3. 错误恢复能力不足：当前实现在遇到第一个错误时就抛出异常终止，无法继
   续分析后续代码以发现更多错误。

4. 缺少行内错误定位：错误报告只包含行号，没有列号信息，不利于精确定位。

5. 科学计数法处理：当 E 后面不是有效数字时的处理不够完善，可能产生意外
   行为。

【建议】
针对上述问题和实验经验，提出以下改进建议：

1. 优化文件读取：使用字符流缓冲技术，避免重复读取文件。

2. 简化状态机：重新设计十六进制数的识别逻辑，减少状态数量。

3. 增强错误恢复：实现 panic mode 或短语级恢复策略，使编译器能够在遇到
   错误后继续分析。

4. 改进错误报告：增加列号信息和更详细的错误描述，提供可能的修复建议。

5. 添加更多测试用例：特别是针对边界条件和错误情况的测试。

6. 扩展功能：可以实现语义分析、中间代码生成等后续阶段，完成完整的编
   译器前端。

【使用 AI Copilot/Vibe Coding 的过程】
本次实验中，我使用了通义千问代码助手（Qwen Coder）来辅助完成实验报告和
代码理解工作：

1. 代码理解辅助：
   - 通过分析项目结构，快速定位 Scanner 和 Parser 的核心代码
   - 帮助理解复杂的数据结构关系，如 Token 类层次结构
   - 解释递归下降分析器中各个函数的作用和调用关系

2. 报告撰写辅助：
   - 根据代码内容自动生成数据结构的详细描述
   - 整理和归纳核心算法的流程和步骤
   - 提供错误处理机制的全面分析

3. 测试用例生成：
   - 协助设计覆盖各种语法结构的正确测试用例
   - 生成包含典型错误的测试用例，用于验证错误处理能力
   - 提供边界案例的建议，确保测试的全面性

4. 使用体验：
   - Qwen Coder 对 Java 代码的理解较为准确，能够快速识别代码模式
   - 在解释编译器概念时，能够提供清晰的技术术语和准确的描述
   - 对于递归下降分析等专业概念，能够给出符合学术规范的解释

5. 局限性认识：
   - AI 助手无法替代对代码的实际运行和调试
   - 某些特定实现的细节仍需人工仔细阅读代码确认
   - 测试结果的预测需要结合实际运行验证

总体而言，AI 编程助手在本次实验中起到了很好的辅助作用，大大提高了代码
理解和文档撰写的效率。但核心的思考和分析工作仍需人工完成，AI 更多是作
为一个高效的"副驾驶"角色。

================================================================================
                              实验报告完成
================================================================================
